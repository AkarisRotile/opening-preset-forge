# 始弦的魔法大典 · destiny 开局预设工坊

SillyTavern（酒馆）/ Tavern Helper 浏览器扩展「悬浮窗」· 当前版本 **v1.10.8**

为「始弦的魔法大典 + 命定之诗与黄昏之歌」一类角色卡提供**开局预设一键流水线**：

> 技能 → 装备 → 道具 → 资产 → 背景 → 汇总开局预设 JSON → 一键导出 `destiny_*.preset.json`

整个流程调用**酒馆当前主 API**（`SillyTavern.getContext().generateRaw`）：插件不保存、不使用任何 API Key，模型与采样参数跟随酒馆现有主 API 配置。

---

## 一、功能总览

- **分步生成初稿**：按 技能→装备→道具→资产→背景→汇总输出 顺序串行调用主 API，步骤卡逐一亮起；
- **栏目生成规范内置**：技能/装备/道具/资产 的基础提示词并入《技能装备道具生成规则》（字段/标签/消耗/效果/徽记/总空间/结算/内部资产等格式约束）；
- **生成后自动合规自检修复**：初稿/重汇总完成后自动按该规范对四类条目做一步自检并修复不合规项（可勾选关闭）；
- **每步独立精修**：每步卡下有「精修框」——可自填方向，或点「该步建议」让 AI 只针对本步给 2~3 条方向；精修只改该步、单线程不并发，改动标「✦ 本步已修改」；
- **⟳ 重新汇总**：各步都改完后，基于最新分步内容重建 P6 汇总 JSON 并自动解析校验；可多轮“改一步→再汇总”；
- **⚡ 快出模式（单次）**：所有阶段合并为一次调用，直接出最终 JSON（省 token，适合初稿草拟）；
- **世界规则·创作限制注入**：生成与精修全程注入提炼自世界书《角色生成》《角色辅助指导》及各“之书”条目的限制（属性公式、等级/层级、品级消耗、资产/伙伴字段等），防止乱写；
- **世界书勾选制**：导入只“登记”条目，默认仅勾选 `constant` 常驻；**只有勾选的条目才会发进 AI 上下文**；
- **世界书自动分类**：按板块分组显示，支持板块/地区展开收起、全开/全关、搜索与分类筛选（详见“四”）；
- **一键导出**：与 `destiny_AI一键生成开局预设格式.preset.json` 同构（可含顶层 `name/createdAt/updatedAt`，时间戳自动填当前值），另支持一键复制；
- **UI**：红魔风 + 辉光全屏多页工作台（v1.7.0 起，手机端适配）；主窗/字号可调（当前字号档位见“五·界面说明”）。
- **🧠 网游核心·文本总结（独立模块，默认关闭）**：开启后每次 AI 完成输出就自动把最新对话压缩成一段 ≤800 字的 YAML 状态摘要，并在下一次生成时作为提示词注入；专门适配《网游核心YAML版》——同名 任务/副本/高难本 跨挑战保持一致（怪物分布/BOSS结构作为固定蓝图不漂移）；高难开荒（歼殛战/零式大型/绝境战）只记队伍已确认的机制与进度，未见阶段留「未知」不剧透；界面提供可视化视图与 YAML 原文切换；摘要按「角色 + 对话」单独持久化到对话文件（切换/刷新自动恢复）；与开局预设生成完全隔离、互不影响。
- **🖥 全屏多页工作台（v1.7.0）**：悬浮窗升级为全屏界面（兼容手机端），顶部标签分页：① 开局预设 ② 世界书 ③ 二创角色 ④ 命定系统 ⑤ 正则工坊 ⑥ 与始弦聊天 ⑦⑧⑨ 占位页（留给后续功能）；
- **✦ 命定系统工坊（v1.8.0）**：按世界书《命定系统》条目的真实结构（包裹标签 + 四节正文 + 十个 setvar 变量槽）设计的分段式创作流水线；**最终输出为「世界书条目正文 + 条目元数据」，并自动跑内置 lint**（标签名一致性/变量槽齐全/消耗档位/撞名/复活约束句等，详见“六·B”）。
- **🧩 正则工坊（v1.9.0 起）**：由命定系统核心产出「对话美化正则」——匹配式由插件确定生成、替换体由模型按档位产出；**v1.11.0 起自检后可直接「🔧 自动修复」（本地纯字符串 + 剩余项交 AI 逐条改写），生成后还能写一句自然语言要求让 AI 「✨ 修改」（只改样式时 HTML 骨架与 `$n` 引用锁定，手写替换体自动切整段模式），改完自动重跑自检与预览**（详见“六·D”）。
- **💾 本地缓存（v1.7.0）**：世界书导入、勾选状态与二创角色草稿都写入浏览器 localStorage——上次启用了哪几本世界书、勾了哪些条目，重开酒馆后自动恢复；重新导入同名文件 = 刷新内容但保留勾选；
- **✒️ 二创角色工坊（v1.7.0）**：按世界书《角色生成》《角色辅助指导》《角色命名指导》《技能装备道具生成规则》等条目设计的分段式角色创作流水线；**最终输出为固定 schema 的纯 YAML 文档**（v1.7.1 规范），可直接粘贴进世界书 DLC 条目（详见“六·二创角色工坊”）。

## 二、安装

### 方式 A：酒馆网页端一键导入（推荐）
1. 顶部 **Extensions ▸ ⬇ Install extension**；
2. 粘贴仓库 URL：`https://github.com/AkarisRotile/opening-preset-forge`（分支默认 main）；
3. 安装 → 按提示刷新页面 → 右缘出现 **✦ 红色魔符**即成功。

> 说明：该方式需要运行酒馆的电脑装有 git；GitHub 访问不畅时可把本仓库导入 Gitee/GitLab 后粘贴对应 URL。
> 若升级后异常：Extensions ▸ Manage extensions → **Delete 后重装**（优先于“Update”），再 Ctrl+F5。

### 方式 B：手工目录（SillyTavern ≥ 1.12）
1. 关闭酒馆；进入 `data/<你的用户名>/extensions/`，新建文件夹 `opening-preset-forge`；
2. 放入 `index.js`，并新建 `manifest.json`：

```json
{
  "display_name": "始弦的魔法大典",
  "loading_order": 50,
  "js": "index.js",
  "author": "you",
  "version": "1.7.6",
  "homePage": "https://github.com/AkarisRotile/opening-preset-forge"
}
```

3. 重启/刷新酒馆。

### 方式 C：手动 zip
仓库附带 `opening-preset-forge.zip`（内含顶层 `opening-preset-forge/` 目录），解压到 `data/<用户名>/extensions/` 即可。

### 卸载
删除对应扩展目录并刷新页面即可。

## 三、快速上手
1. 点 ✦ 打开全屏工作台，在「① 开局预设」页填**开局需求**（如：给流浪剑士配 1 级开局、偏好近战、带契约伙伴）；
2. （可选）切到「② 世界书」页，点「导入世界书文件」载入 `命定之诗与黄昏之歌v4.3 (6).json`，按板块勾选要发送的条目（导入与勾选自动缓存，下次打开还在）；
3. 点 **▶ 生成初稿**；每步完成后可在卡片下做**分步精修**；
4. 全部满意后点 **⟳ 重新汇总**，观察“预设 JSON”区的解析与校验提示；
5. **⬇ 导出 .preset.json** 或 **⧉ 复制**；
6. 写 DLC 角色则切到「③ 二创角色」页（见“六·二创角色工坊”）。

## 四、世界书（重点）

### 加载与勾选
- **多本世界书可叠加导入**：反复点“导入世界书文件”即追加；同一文件重复导入 = 刷新该书条目（不重复、保留该书已做勾选）；
- 世界书页顶部状态行显示：已勾选 X/N 条 · 约 Y 字会发送给 AI · 已载 N 本世界书；
- 多本并存时每行带**来源书小标签**；
- 兼容任意“按 `[DLC][角色]`、`[本体][分类]` 等格式命名”的其它世界书；也支持自动探测酒馆当前激活世界书（部分环境可用，失败请用文件导入）。

### 自动分类板块
导入后条目自动归入下列板块（板块标题可点击展开/收起，右侧「全开/全关」一键操作该板块全部条目；顶部有 全选/仅常驻/清空勾选 与 搜索/分类下拉）：

| 板块 | 说明 |
| --- | --- |
| 命定系统 | 命定系统-xx核心 / 命运抽卡 |
| 规则·系统 | 世界规则/角色生成/战斗·生产协议/数值与状态/复活/好感/登神等 |
| 导览开关 | ➡️…开始/结束 分组开关、各类概览 |
| 地区·地理 | 国家/大地区条目（内部再按地区二级索引，见下） |
| 种族 | 非 DLC 种族 |
| 角色 | 角色卡/命名指导 + **DLC角色已并入** |
| 组织·势力 | 公会/协会/教会/阴影势力/捕奴队/苍棘之塔/异界信徒等 |
| 经济·产业 | 银行信贷/贸易/工坊租赁/材料采集/锻造装备/魔宠/奴隶等 |
| 节庆 | 节庆_*（各节日） |
| DLC扩展 | `[DLC][扩展]…` |
| DLC事件 | `[DLC][事件]…` |
| 其他 | 兜底（对 v4.3 当前为 0） |

分类规则优先级（v4.3(6) 起）：注释层级标签（`[本体][分类]…` / `[DLC][角色|扩展|事件]…`）优先 → 导览开关(➡️) → 节庆 → 产业 → 组织 → 势力/冒险区域/怪物生态(地区) → 种族 → 角色 → 世界设定/额外设定/变量/COT/事件(规则) → 控制/快捷功能(导览)。
旧版（v4.3(3)）及其它世界书回落旧链：DLC 标签 → 标题例外表（组织12条/产业15条，可继续扩充）→ 命定系统 → 规则关键词 → 导览 → 种族/角色 → 节庆 → 地区头段/地理词 → 组织词 → 规则词 → 经济词 → 其他。
需要修正某条目的归属，可在本仓库提 issue，把“注释名 + 期望板块”列出即可（也可做成设置里可编辑的例外表）。

### 「地区·地理」二级索引
展开该板块后先显示**国家/大地区索引**（v4.3(6) 取 `[本体][势力][精灵王庭]…` 中“势力/冒险区域/怪物生态”后的下一级标签；旧版取方括号内第一段，如 `[精灵王庭-…]` → 精灵王庭），按地区名排序并显示条数与已勾数；点地区名再展开其条目（默认收起为索引视图，避免几百条刷屏）。

## 五、界面说明
- **✦ 魔符**：屏幕右缘入口（启动时最先创建，可拖动；不依赖其余 UI），点击开合全屏工作台；
- **全屏工作台**：红魔风 + 辉光；顶部标题栏 + 可横滑的标签页导航；主体按页切换：① 开局预设（原主窗全部功能）② 世界书（原侧栏迁入，勾选/板块/地区索引/搜索不变）③ 二创角色工坊 ④ 命定系统工坊 ⑤⑥⑦ 占位页；内容区内部滚动，手机端（≤760px）自动加大触控目标与字号；
- **开局预设页**：开局需求输入 / 选项行 / 创作步骤（技能→装备→道具→资产→背景→汇总）/ 分步精修 → 重新汇总 / 预设 JSON 预览 / 底部动作按钮；
- **世界书页**：顶部工具行（全选/仅常驻/清空勾选/筛选/分类下拉）+ 板块分组 + 地区二级索引 + 全开/全关；导入与勾选自动缓存；
- **二创角色页**：需求/参考输入 + 分段步骤卡（7 段，各带定点精修）+ 交火梳理报告 + 最终稿件（标签纯文本，可复制）；
- **命定系统页**：需求/参考/署名输入 + 分段步骤卡（7 段或 10 段，各带定点精修）+ 交火梳理报告 + 最终稿件（YAML 框架 + 条目正文，附 lint 提示）+ 复制正文/复制 YAML/重新自检/去装饰；
- **正则工坊页**：核心全文输入 + 语言格式解析核对 + 多条正则草稿卡（用途/档位/匹配式/替换体/测试文本）+ 实时渲染预览框 + 自检（每条问题带「改正则」「改核心」）+ 导出单条与批量 JSON；
- **与始弦聊天页**：顶部面板专属设置（你的名字/她的自称/世界书来源/注入上限/向量接口与模型/检索参数）+ 对话流 + 输入框 + 压缩记忆/导出记忆/清空 + 本面板独立世界书勾选列表 + 本地记忆统计；
- 字号档：主窗约 12–14.5px（比初版放大过一档）；若还想调可改设置或告知。

## 六、二创角色工坊（v1.7.0 新增，v1.7.3 内容规范改版）
为世界书写 **DLC 角色条目**，先用「角色」试水，流程四步：

1. **分段初稿**：按世界书条目敲定的 6 段串行生成——① 定位与基础（命名/种族/层级/身份）→ ② 性格与动机（性格码/行为逻辑）→ ③ 外貌与衣着 → ④ 战斗配置 → ⑤ 背景与经历（实力来源/关系锚点）→ ⑥ 演绎与语料（语料示例/禁忌/提倡）。每段只写自己，已定段落作为既有设定必须一致；
2. **⚔ 交火梳理（两步式）**：**第一步**只输出梳理报告与改动清单（不重写段落，输出小、不截断）——按联动链条审查（`出身/经历 → 性格 → 外貌/衣着/战斗方式 ↔ 战斗配置 → 演绎语料`，L1 实力有来历 → L2 经历塑性格 → L3 命名匹配种族阶级 → L4 性格映照外貌 → L5 性格+层级定战法 → L6 战法定技能装备 → L7 性格+背景定语料，另含反向校验：技能来源在经历中可查、登神长阶按等级档位、唯一品质仅限出处特殊）；**第二步**按报告逐段单独调用应用修订（每段只输出该段全文，无改动回复"无改动"，杜绝一次输出全部段落被截断）。两个阶段都硬性锁定：武器/装备/道具/技能保持 名称/品质/叙述 三段式，不得补回 类型/消耗/标签 字段；
3. **定点修改**：每段卡片下有精修框——用户指令只作用于该段，其它段作为「冻结区块」原样保留（模型不得代改，最多末尾留一句备注）；也可点「该段建议」生成 2~3 条方向；
4. **🎁 最终封装（YAML 规范）**：以“始弦的魔法大典”的身份，把 6 段整合为一份固定 schema 的 **纯 YAML 文档**（顶层唯一键 `角色卡`）。**v1.7.3 内容规范**：
   - 不写「面板」：五维/HP·MP·SP 不写入条目，由游玩时按世界规则自行结算；
   - 武器/装备/道具/技能每项只写三条：**名称、品质（中文七等：普通/优良/稀有/史诗/传说/神话/唯一）、叙述**（一段文字，先写效果再写描述，不单列类型/消耗/标签）；
   - 不预设对 user 的态度（每位用户的设定不同），只写角色自身的互动方式；
   - 两空格缩进、禁 Tab、`|-` 块标量、` ```yaml ` 代码块输出；封装后自动做轻量 YAML 自检（Tab/顶层键/括号配对）。

**用词与文风规范（内置，全程注入）**——为压掉模型“八股”，按九条约束写：① 少用“而是/名为/被称为/取而代之”等连词腔，转折直接换句；② 删“极其/极度/极为/无比/令人××”空转副词，用细节替代强度；③ 禁“底层逻辑/张力/解构/本质/主体性”类论文术语与自由解放、压迫凝视类大词；④ 禁一切比喻类比（“像/如同/仿佛/犹如/好似”及心湖涟漪、手术刀教科书类意象说法），是什么就写什么；⑤ 禁“冷笑/冷哼/嘴角勾起弧度/指节泛白/不容置疑”等网文套路与成语堆砌；⑥ 禁“征服/支配/弱肉强食/丛林法则/内卷”等口号式评判，写动机行动不下判词；⑦ 不写“他的声音/她的语气/这番话”类语音提示旁白；⑧ 禁“不是A而是B”“没有A只有B”“并非A而是B”等否定-转折/排除-定义句式与“名为X”句式，同一句式一段内不重复；⑨ 少用括号解释与引号强调。

草稿（需求/分段/报告/最终稿件）随本地缓存持久化，刷新页面自动恢复；「🗑 新角色」清空重来。

**隔离声明（v1.7.5）**：二创角色与开局预设是两套完全独立的提示词——各自独立的角色口吻、规则块与任务说明；二创流程不注入任何开局预设规则（开局等级、开局剧情、面板、伙伴、资产等一概不出现）。唯一共用的是「用词与文风规范」（你指定两边生效）和世界书页勾选的条目数据。

## 六·B 命定系统工坊（v1.8.0 新增）
为世界书**「命定系统」板块**写一个新的「XX核心」条目（即二创命定系统）。流程与二创角色同构，但换成命定系统自己的规范：

**先做准备（关键）**：切到「② 世界书」页勾选喂给 AI 的条目——规则·系统板块里必须有 `登神长阶`、`生命层级与社会阶级`、`技能装备道具生成规则`、`经验值获取`、`复活机制`、`战斗&生产规则`、`角色生成`、`变量更新规则`；命定系统板块**只留 1 个范本核心**（推荐 `梅林核心`＝最规范的散文式模板，想走数据结构路线可选 `读者核心`）。**其余核心务必取消勾选**：22 个核心默认常驻，全勾约 20 万字符，会直接把上下文撑爆。注意插件勾选只影响"发给 AI 的上下文"，成品条目最终要自己在酒馆世界书里开关（现有 22 个核心出厂即 `disable: true`，玩家只开自己那一个）。

1. **分段初稿（7 段串行）**：① 骨架与命名（系统名/系统核心名/系统核心/一句话概念/原型出处，本段定名后其余段不得再改）→ ② 契约与核心机制（契约/核心机制/隐蔽原则/缔结条件/结果反馈）→ ③ 命运点数(FP)（概念/获取规则/消耗规则/范例）→ ④ 命定之灵（开场白/人设/功能表/规则/缔结范例）→ ⑤ 语言格式（专属 XML 标签/参数枚举/6 条以上范例）→ ⑥ 十个变量槽 → ⑦ 可选扩展（自带命运抽卡条目 / 额外变量钩子）。每段只写自己，已定段落作为既有设定必须一致；
2. **⚔ 交火梳理（两步式）**：与二创角色相同的“先出报告、再逐段应用”结构，链条换成命定系统自己的 8 条——L1 命名链（系统名 = 包裹标签名）、L2 概念链（概念 ↔ FP 概念 ↔ 获取规则）、L3 契约链（七档消耗 ↔ 情感修正 ↔ 结果反馈）、L4 灵格链（开场白 ↔ 人设 ↔ 功能表 ↔ 缔结台词）、L5 功能链（功能表 ↔ 规则落点）、L6 输出链（语言标签 ↔ 灵格）、L7 锚点链（不得越权发要素/权能/法则、复活机制不得战斗中无敌）、L8 预算链（4000~8000 字符）；
3. **定点修改**：每段卡下精修框，只改该段、其它段冻结；也可点「该段建议」；
4. **🎁 最终封装**：模型只产出**条目正文**（` ```text ` 代码块），**条目元数据由插件确定生成 YAML 框架**——输出区显示的就是一份完整 YAML 文档：

```yaml
命定系统条目:
  注释名: "[本体][命定系统]<系统核心>(<署名>)"
  constant: true
  position: 4
  depth: 1
  order: 1100
  disable: true
  selective: false
  probability: 100
  key: []
  系统名: …
  系统核心名: …
  系统核心: …
  fp定义: …
  篇幅: 5123
  正文: |-
    <系统名>
      …（条目正文原样，四空格缩进）
    </系统名>
    {{setvar::系统名::…}}
```

　　按钮：**⧉ 复制条目正文**（只复制去缩进的正文，直接粘进世界书条目）、**⧉ 复制完整 YAML**、**🔎 重新自检**、**🧹 去装饰**（清掉正文里的 markdown 标记，见下）。「署名」输入框决定注释名里的 `(<署名>)`，留空写「未署名」。

**关于 `#` 与 `*`**：命定系统条目是 YAML 风格的数据文档，不是 markdown 报告。但模型很容易写成 markdown，原因是三个：① 注入的世界书参考条目（《技能装备道具生成规则》有 15 个 `#` 标题、47 个列表项；《经验值获取》《角色生成》各 3 个）本身是 markdown 风格的规则文档，模型照抄了它们的排版；② 既有核心的成品语料里本来就有 `#` 和 `**`（19/22 个含有 `# {体系名}的加护`，11 个含 `**加粗**`）；③ 早先的约束只说了"正文块里不放 markdown"，既没管正文块之外，也没有 lint 兜底。现在三层都堵上了：**《输出格式·硬约束》全程注入**（禁 `#` 标题、`**`/`*` 强调、`*`/`+` 列表符、`---` 分隔线、markdown 表格；列表统一 `-`）、**5 项装饰 lint**（跳过 setvar 槽位，槽内的 `# 体系名的加护` 是既有惯例不算违规）、**🧹 去装饰**一键清理（保留槽位内写法）。

**内置 lint（纯文本自检，只提示不拦截）**——11 项：
- 包裹标签名与 `{{setvar::系统名}}` 是否一致（不一致则《战斗&生产规则》读不到「核心机制」）；
- 十个 setvar 是否齐全、顺序是否正确、`{{setvar::` 与 `}}` 是否配对（EJS 动态写入的核心会提示“可忽略”）；
- 缔结契约消耗档位是否 6 档齐全且随层级递增（默认档 200/500/2500/10000/50000/150000；用自定义档会提示确认，实测世界书内并存 500/2000/6000/20000/60000/200000 与 200/500/2500/**5000**/50000/150000 两套写法）；
- 是否混入英文品级（要求中文七等）；
- `系统核心` 是否与现有 18 个核心重名、语言标签是否与现有 `nailong/giraffe/Foreigner/Foreigner_alter/lilith/xianzu/dalian` 冲突；
- 是否缺「隐蔽原则」「非万能原则」、复活机制是否保留“禁止为<user>在战斗中改变设定/创造有利条件/机械降神”；
- 正文长度是否落在 4000~8000 字符（上限 12000）；
- 是否误混入角色卡/开局预设字段。
- **标准骨架**：人设六字段（形象/性格/定位/说话风格/爱好/愿望）是否齐全、核心机制与命运点数(FP)节是否存在、是否含 Tab；
- **功能表**：条数是否在 7~11、是否覆盖标准八项功能族、消耗标注是否为 `(消耗FP)`；
- **语言格式**：范例是否 ≥6 条、XML 标签式是否配了「参数说明」。

**📐 标准速查**：页面上的「📐 标准速查」按钮会展开内置的《命定系统标准》运行时版（三层结构 / 包裹标签规则 / 四节标准名 / 人设六字段 / 功能八项词表 / 语言格式两族 / 契约数值 / 样式规范 / 十槽顺序）。该标准由世界书内 **22 个命定系统条目**逐条普查提炼，人类可读全文见仓库内《命定系统标准.md》。

**隔离声明**：命定系统与开局预设、二创角色是**三套独立提示词**（各自的口吻、规则块、任务说明与状态对象）；唯一共用的是「用词与文风规范」和世界书页勾选的条目数据，草稿各自持久化于 `openingPresetForge_destinydraft_v1`。

---

## 六·C 进阶（可选）· EJS 重型核心（v1.8.0 新增）

给「命定系统」页勾上 **🧬 进阶（可选）：EJS 重型核心** 即开启。**默认关闭**，开启后基础 7 段不动，只在末尾追加 3 段，并把 EJS 规范注入系统提示——基础流程完全不受影响。

**什么时候才该开**：静态散文 + 十槽已能覆盖世界书里 18/20 个现有核心（梅林核心 4.9k 字符就是范本）。只有需要「按存档状态改变输出 / 多形态运行时切换 / 渲染时顺手写变量 / 人格可被玩家配置 / 条件触发规则」时才值得上。代价要知情：**直连主 API 的模式（含本插件自己的生成流程）不执行世界书里的 EJS**，生成期看不到渲染结果，只能静态检查；变量重名会让整段编译失败；体积通常是常规核心的 4~15 倍。

### 追加的 3 段

| 段 | 内容 |
|---|---|
| **EJS·守卫与数据层** | 块作用域 `<%_ { _%>` ／ 身份守卫 `getvar('系统核心') === '<系统核心>'` ／ 数据读取（`getvar` `getMessageVar` `getLocalVar` `getChatMessage` `matchChatMessages` `TavernHelper.*`）／ 防错归一（枚举白名单、`Number.isFinite`、类型守卫、玩家配置文本净化）／ 优先级裁决（存档优先于配置） |
| **EJS·派生与渲染层** | 派生写回（`setMessageVar(..., { scope: 'message' })`，幂等防重复累加）／`insertOrAssignVariables` 自动登记 NPC／`<{{getvar::系统名}}>` 包裹 + `<%_ if _%>` 条件分支渲染／标签配对 |
| **EJS·引擎与兜底** | A 声明式条件机制引擎（5 数据源 × 14 运算符 × 5 类型动作白名单、路径白名单、capture/restore、FNV-1a 指纹幂等、消息编辑回滚、事务上限）／ B 配置驱动人格（`dream_custom_persona` JSON + `_custom_text` 取值器）／ C 静默与兜底（唤醒条件 + 两条分支都闭合标签） |

### 🧬 EJS 速查

页面上的「🧬 EJS 速查」按钮展开内置的运行时版规范（源自对**艾莉亚核心** 666 行 / 28 个 EJS 块与**读者核心** 1598 行 / 74 个 EJS 块的逐行逆向）。人类可读全文见仓库内《命定系统EJS进阶规范.md》，含六层架构、API 速查表、引擎 schema、反模式清单与可直接复制的骨架模板。

### EJS 专用 lint（8 项，只提示不拦截）

- **花括号净差**：把所有「代码标签」拼接后扫 `{`/`}` 净差（跳过字符串/注释/正则），非 0 即告警（实测两个重型核心均为 0，判定准确）；
- **`<%` 与 `%>` 数量**是否配对；
- 是否用了最外层块作用域 `<%_ { _%>`；
- 是否有身份守卫 `getvar('系统核心') === …`（读者核心那种"就是要覆盖槽位"的设计会提示可忽略）；
- 是否用了 `<{{getvar::系统名}}>` 作包裹标签（自动跟随系统名）；
- `setMessageVar` **第三个参数**（`{ scope: 'message' }`）是否缺失——按括号配对解析实参个数，传变量（如 `_write_options`）不会误报；
- 读到数值却没有 `Number.isFinite` 守卫；`new RegExp(` 没有 try/catch；
- **包裹标签开闭数量**是否相等（某条条件分支漏闭合会被抓出，这是最隐蔽的一类崩坏）；
- 越界调用（`eval` / `new Function` / `fetch` / `XMLHttpRequest` / `localStorage` / `document.` / `import(` / `require(`）。

**验收数据**：`node tools/test-destiny.mjs` 会跑「20 个真实核心的基础 lint + 2 个重型核心的 EJS lint + 8 个反例夹具 + 1 个干净夹具」。当前结果：艾莉亚核心 EJS 警告 1 条（仅"建议改用动态包裹标签"）、读者核心 2 条（无身份守卫 / `<%- %>` 建议确认净化）、反例命中 8/8、干净夹具 0 误报。（该验收脚本与普查原始数据保留在开发工作区，未随仓库分发。）

---

## 六·D 正则工坊 · 命定系统对话美化（v1.9.0 新增）

给一个命定系统核心产出**对话美化正则**——让 `<nailong tag="唐">…</nailong>` 或 `> 梅林:「…」` 在消息里渲染成带边框、配色与动效的对话框。

**核心设计：匹配式由插件生成，替换体由模型产出。** 匹配式是"对不对"的问题，必须确定；替换体是"好不好看"的问题，才需要模型。这样避免了模型把正则写错却看着像对的经典翻车。

1. **输入核心**：把核心条目全文粘进来，或点「⬅ 从 ④ 页带入」（直接取 ④ 页最终封装出来的条目正文）；
2. **🔍 解析语言格式（AI）**：由 **AI 负责理解、脚本负责精确与校验**。AI 把「语言格式」节读成结构化 JSON（标签/说话人/`name` 固定值/参数与枚举/引号形态/内嵌标签/用途建议/范例/疑点），脚本随后做**四道校验**——① **范例必须逐字存在于核心原文**（AI 最常见的毛病是"顺手润色范例"，这条直接丢弃编造项并记入疑点）；② 标签名合法且不在 HTML 黑名单（防止又把 `<h2>` 当对话标签）；③ 枚举去重去空、参数名合法；④ 用抽出的范例**当场试跑**生成的正则，命中率不足即提示"AI 解析可能不准"。AI 不可用 / 返回不是 JSON / 解析出 0 套格式时**自动回退**到「⚙ 脚本解析（离线）」按钮那套纯正则解析，结果面板会标明用的是哪个引擎。

   两种引擎都能吃**两种写法**——markdown 标题式（`### 崩坏神语言格式` + `**格式**` + ```xml 代码块）与缩进式（`  命定之灵语言格式:` + `格式: '> 梅林:「 {对白} 」'`），也都支持**一个核心多套格式**（类脑娘的"常规模式"与"大字报模式"会各成一条正则；高文与 UNN 的多说话人同理）。结果面板只读展示，供你核对；
3. **勾选与配置**：每套格式一张卡，可选用途（对话美化/登场/缔结契约成功/命运抽卡/咏唱专属块/自定义）与**预算档位**；
4. **🎨 生成替换体**：模型按 12 条硬约束 + 档位目标字数产出 HTML，情绪参数自动落成 `data-mood="$2"` 并用属性选择器分支配色——**一条替换体覆盖全部枚举**，不必为每个情绪写一条正则；
5. **实时预览框**：贴着编辑区，左改右看，直接显示渲染后的方框与「匹配 N 处」，输入默认取该核心自带范例；
6. **🔎 自检 + 🔧 自动修复（v1.11.0）**：自检覆盖编译失败 / 缺 `g` / `$n` 引用超出捕获组数 / `.*` 不跨行 / 回溯炸弹 / `<script>` / 外部资源 / 写死像素宽 / mood 未分支 / uuid 与命名 / `placement` 与现网不一致 / 双 true / 体量偏离档位 / 正则匹配不上测试文本 / **核心范例与正则不一致**。**每条问题都带「改正则」与「改核心」两个入口**，点哪个改哪个；

   **自动修复分两级，先便宜后贵**：
   - **本地修复（纯字符串、瞬时、不花 token）**——自检区顶部「🔧 一键自动修复」与每张卡的「🔧 只修这一条」会先把能确定修掉的一次性处理完：补 `g`、`.*` → `[\s\S]*?`、剥离 `<script>`（同时清理样式层与替换体，不会"修完又被骨架拼回来"）、重发 uuid、`promptOnly` 归位、`placement` 归位；
   - **AI 修复（需要改写才可能修好的项）**——`$n` 引用越界、mood 未分支、外部资源、写死像素宽、匹配不上范例、体量明显偏离、核心范例与正则不一致、替换体为空。本地修完后弹一次确认（列出本地已修项与剩余项数），确认后**按条目分组合并成一次调用**逐条改写，每条只带该条自己的问题清单，其余条目冻结，改完自动重跑自检。
   - 自检区每条可改写的问题还各带一个「✨ AI 修这条」，只想修一条时不必跑全量。
7. **✨ 生成后修改（v1.11.0）**：每张卡在替换体下方有修改行——左侧选择范围（**只改样式** / **样式+匹配式**），右侧写一句自然语言要求（例："边框改成暗金色、字号大一点、去掉动效"、"台词外的括号改成灰色小字"），回车或点「✨ 修改」即执行；点「💡 建议」则由模型先给 2~3 条**只针对样式**的具体方向（做成可点的小按钮，点一下即作为要求执行）。

   **两种模式自动切换，防止改坏结构**：条目是"骨架 + CSS 分离"产生的（插件生成的），只让模型回传并替换 **CSS 层**，**HTML 骨架、class 前缀与全部 `$n` 捕获组引用原样锁定**，因此不管怎么改都不会把结构改塌；而你**手写**的替换体（没有插件骨架）会自动切成**整段替换**模式，让模型直接输出改好的整段 HTML，绝不拿一个空骨架去覆盖你的手写内容。改完自动重跑自检并刷新预览。
8. **导出**：`⧉ 复制单条 JSON` / `⧉ 复制 JSON 数组`，字段名与你现有 4 条正则完全一致（`scriptName` / `findRegex` / `replaceString` / `placement` / `markdownOnly` / `promptOnly` / `runOnEdit` / `substituteRegex` / `minDepth` / `maxDepth` / `trimStrings` / `disabled` / `id`），可直接粘进预设的 `regex_scripts`。

**预算档位**（目标值，不是硬上限；允许 ±30% 浮动）：

| 档位 | 目标字数 | 取向 |
| --- | --- | --- |
| 极简 | ≈ 1,500 | 单色边框 + 说话人 + 台词 |
| 轻量 | ≈ 3,000 | 边框 + 标签 + 少量装饰 |
| 标准 | ≈ 6,000 | 主题配色 + 轻动效 |
| **精致（默认）** | **≈ 10,000** | 对标现网主力：月蚀对话美化 7,957 / 飨宴 11,997；SVG 装饰 + `@keyframes` + mood 分支配色 |
| 豪华 | ≈ 20,000 | 多层结构 + 入场动效 + 细节纹理 |
| 不设限 | 按需 | 模型自行发挥（参考现网 40k 级的艾莉亚车票）；**不做任何长度拦截** |

**12 条硬约束**（生成替换体时注入）：不动匹配式与捕获组含义；`markdownOnly: true` + `promptOnly: false` 且不得同时 true；最外层 div 内自带 `<style>`，class 前缀唯一（插件给定）；禁 `<script>` 与一切外部资源，字体只用系统字体栈；禁写死像素宽，容器 `max-width:100%`；正文里的 `$` 写成 `$$`；情绪用 `data-mood` 分支配色；不留裸 `>` 引语；动效周期 ≥2s；按档位目标字数写；配色贴合世界观与灵格；只输出一个 ```html 代码块。

**实测覆盖**（`node tools/test-regex-workshop.mjs`）：世界里 22 个命定系统条目中，**18 个可生成且自带范例 100% 命中**（含类脑娘双模式 2/2 + 1/1、UNN 双说话人 3/3 + 2/2）；3 个本来就没有语言格式节（null核心与两条命运抽卡条目，非对话核心），1 个是读者核心的**无标记裸引号格式**——自动生成会吃掉全文所有 `「」`，因此**拒绝生成**并在自检里建议把核心改成带标记格式。

**注意**：本页只产出 JSON 文本，**不会**自动写入你的预设或酒馆；导入与启用仍由你在 SillyTavern 正则面板完成。

---

## 六·E 与始弦聊天 · 一起旁观世界书（v1.10.0 新增）

一个**完全独立**的对话面板：她是你在大图书馆的司书，也是同伴——你们并肩站在书库外，一起看某部世界书描绘的世界。

**隔离是硬承诺**：自有消息数组（不写酒馆聊天记录、不触发你的正则与 MVU、不写存档）、自有 `{{user}}` 称呼（面板内单独设置，不动 `getContext().name1`）、自有世界书选择（与其它板块的勾选互不影响）、自有存储。

**设置**（面板顶部，全部只属于本面板）
- **你的名字**：本面板专属称呼，只替换本面板提示词里的 `{{user}}`；
- **她的自称**：默认「始弦」；
- **世界书来源**：`面板内选书`（导入文件 / 快照酒馆激活书）或 `酒馆激活书`；
- **注入上限**：默认 12,000 字符（防上下文爆炸）；
- **向量接口 / 密钥 / 向量模型**：自定义 OpenAI 兼容接口——填 Base URL（如 `https://api.example.com/v1`）与密钥，点「🔌 读取模型列表」拉取 `/models`，优先自动选中名字像 embedding 的模型（`embed`/`bge`/`m3e`/`text-embedding`/`gte`/`jina`）。密钥只存在本机设置里，**不写入任何日志**；
- **检索条数 / 相似度阈值 / 窗口轮数**。

**记忆三层**
1. **滚动窗口**：最近 N 轮原文（默认 8 轮）直接进上下文；
2. **记忆条目**：点「🧠 压缩为记忆条目」（或每 N 轮自动）把窗口外的对话压成 `标题 / 要点 / 关键词 / 情绪`，与原文一起入库；
3. **向量检索**：当前输入 → embedding → 与本地库算余弦 → 取 top-K（带阈值）→ 作为「回忆片段」注入，面板上会显示本轮引用了哪几条、相似度多少。

**存储**：设置存 localStorage（很小），**消息 / 记忆条目 / 向量存 IndexedDB**（库名 `openingPresetForge_shixian`）——世界书缓存已经占掉 4.5MB localStorage 配额，向量必须另存。可「⧉ 导出记忆」整包复制（含消息/记忆/向量/配置，注意其中含向量接口配置），或「🗑 清空对话与记忆」一键清库。

**世界书 = 你们一起旁观的那个世界**：面板内可导入多本、逐条勾选。语义是——**勾选 = 总是注入**（★ 为常驻条目），未勾选的不常驻，但仍会被「全馆藏检索」按关键词命中召回（条目 key 命中 +10、注释命中 +6，常驻优先，受注入上限约束）。她的定位是**同伴观察者 + 全馆藏查询**：可以为你们调取还没去过的地区、还没遇到的角色、还没发生的事件，像查资料一样讲解、对照与吐槽；但她不扮演那个世界里的角色，也不推进它的时间线。

**实测**（`node tools/test-shixian.mjs`）：向量哈希/余弦、世界书归一与常驻勾选、全馆藏检索四条用例（精灵城市 → 艾尔文海姆、登神 → 常驻条目、梅林 → 角色条目、无关输入 → 仅常驻）、面板专属宏替换、UI 接线（25 个 id、36 个函数）全部通过。

**已知限制**：自定义向量接口是**浏览器直连**，若你的服务端不发 CORS 头会被浏览器拦下（本地 LM Studio / Ollama / one-api 之类通常允许；遇到拦截会明确报错而不是静默失败）。未配置向量时对话仍可用，只是记忆检索跳过。

**⚠ 关于 524 超时与截断（v1.10.1 架构调整）**：最初是让模型一次吐完整段 HTML（精致档 10k、豪华档 20k），这会撞上 Cloudflare 的 100 秒墙与输出 token 上限——**截断几乎是必然**。现在改成：

1. **结构由代码生成，模型只写 CSS**：骨架（最外层 div、`data-mood="$2"`、说话人 `$1`、正文 `$3`）由插件按捕获组精确生成——**顺带根治了"$n 引用指错捕获组"这一整类错误**（以前是模型最容易写错的地方）；模型只产出 CSS 规则；
2. **按档位分段**：极简/轻量 1 段（≈1.3k/2.6k 字符）、标准 3 段、精致 4 段、豪华与不设限 6 段，每段预算 ≈1.7k~3.5k 字符，从源头把单次输出压到安全区；
3. **截断自动续写**：用**花括号净差**（跳过字符串与注释）判断 CSS 是否写了一半，未配平就带着"已保留部分的结尾"再要一次**剩余部分**，最多 3 轮；
4. **超时自动重试**：识别 `524/502/503/504/timeout/空返回` 等可重试错误，退避重试（1.2s / 2.4s），失败时**保留已生成的部分**并明确报错，可再点一次续做；
5. **砍掉提示词里的整本世界书**：以前每次生成都把 `[世界书参考]`（最多 3 万字）整个塞进系统提示，是延迟的主要来源；CSS 生成改用精简系统提示（只带 500 字气质摘录）。

**实时进度**：页面上的状态行会逐条打印"基础样式 第1次：+1820 字符（14s）""花括号还差 3 个，继续续写…"，524 发生时你能直接看到是哪一段、第几次。

**🚀 生成传输：默认零配置（v1.10.7）**

**为什么"酒馆里明明都是流式"、插件却撞 524？**（v1.10.8 把它查到了请求体那一层）

```js
// public/scripts/openai.js
L2724:  const stream = settings.stream_openai && type !== 'quiet' && !isO1 && ...;
L2751:  'stream': stream,      // 直接进请求体，发给 /api/backends/chat-completions/generate
```

| 路径 | type | 请求体里的 stream | 表现 |
| --- | --- | --- | --- |
| 酒馆正常聊天 `Generate('normal')` | `normal` | **true** | **流式**（你在酒馆看到的就是这条） |
| 插件的 `generateRaw` | `quiet` | **false** | 非流式：中转必须等整段回包，长输出撞 100 秒墙 |

两条是不同的代码路径，所以"酒馆里全是流式"和"插件非流式"同时成立。

**所以流式档现在几乎不用手打**：点「📋 用酒馆的反代设置」——插件直接从酒馆设置面板读 `#openai_reverse_proxy` 与 `#openai_proxy_password`
（依据 ST 1.18.0 `openai.js` L363/L372 的字段绑定，以及 L2788-2791 正是把这两项塞进 `generate_data` 的地方），
自动填好地址与密码、顺手切到流式档，并立刻去拉模型列表。


默认档就是 **「酒馆主 API（零配置·推荐）」**：**不选流式传输时，地址/密钥/模型名这些字段完全不出现**，也不需要填任何东西——
模型与采样参数就是你现在聊天用的那套。自定义字段采用**渐进披露**：只有你主动选了流式档才会显示，状态行会直接告诉你还缺哪一项。
主 API 路径还额外用 `responseLength` 按每段预算硬顶输出长度（防跑飞）；
配置不完整时**不会静默改写你的设置**，只在本次生成降级到主 API 并提示原因。

下面这些只在你想绕开长输出超时（524）时才需要了解：

**🚀 关于 524 与流式（备用档说明）**

报错 `524` 来自中转站 `gcli.ggchan.dev` 前面的 Cloudflare——源站 100 秒内没回任何字节。查本机 ST 1.18.0 源码后确认了一个硬事实：

```js
// public/script.js
L4018:  data = await sendOpenAIRequest('quiet', generateData, ...)      // generateRaw 固定用 'quiet'
L5326:  if (isStreamingEnabled() && type !== 'quiet') { ...流式... }     // 流式分支明确排除 'quiet'
```

**`generateRaw` 永远是非流式的**——不管酒馆里的 Streaming 开关怎么设，插件走这条路都拿不到流式。

**正解：让插件走酒馆自己的服务端转发。** 你用的是类反向代理，反代地址、密钥、Google 协议转换、CORS 本来就该由酒馆服务端处理：

```
POST {origin}/api/backends/chat-completions/generate
body: { chat_completion_source:'makersuite', reverse_proxy:'<你的中转>',
        proxy_password:'<密钥>', model:'gemini-2.5-pro',
        messages:[…], use_sysprompt:true, stream:true, max_tokens:8192 }
```

依据（ST 1.18.0 源码）：`router.post('/generate')` → `case MAKERSUITE: sendMakerSuiteRequest(...)`；常量 `MAKERSUITE: 'makersuite'`；`responseType = stream ? 'streamGenerateContent' : 'generateContent'` 并追加 `&alt=sse`；`convertGooglePrompt(request.body.messages, …)` 会把 system 转成 `systemInstruction`；`apiKey = request.body.reverse_proxy ? request.body.proxy_password : readSecret(...)`。

**「生成传输」三档**（页面下拉）：

| 档 | 走哪条路 | 用途 |
| --- | --- | --- |
| **酒馆主 API** | `generateRaw` | 默认；**非流式**，长输出容易撞 100 秒墙 |
| **经酒馆服务端转发 + 流式** | `/api/backends/chat-completions/generate` + `stream:true` | **反代场景推荐**：无 CORS 问题、密钥与反代地址由服务端处理、Google 协议由 ST 转换、真流式回传 |
| **浏览器直连 + 流式** | 插件直接 `fetch`（OpenAI 兼容 `/chat/completions`，或 Google 原生 `:streamGenerateContent?alt=sse`） | 有 CORS 头的自建/本地接口 |

**模型名不用手填（v1.10.6 改为双通道）**：点「🔌 获取模型列表」会同时走两条路并合并——
① ST 服务端 `POST /api/backends/chat-completions/status`（走反代，无 CORS）；
② **直接向反代地址要原始 `/v1beta/models`**（可能被 CORS 拦，拦了就跳过）。
之所以要第二条：**ST 会用 `supportedGenerationMethods?.includes('generateContent')` 过滤，而新版 Gemini 模型不再返回该字段，会被整类丢掉**——
症状就是"列表里只有 2.5-pro 时代的老模型"。第二条通道不过滤，把新模型全部保留。
另外：**手填过的模型名会被记住**（写进本机配置，下次直接从下拉里选），所以即使两条通道都取不到，你手打一次就永久可用。
状态行会报告三个数字：原始列表 N / ST 过滤后 M / 自定义 K，以及哪条通道失败、为什么。
：点「🔌 获取模型列表」即可——服务端转发档会调 ST 的 `POST /api/backends/chat-completions/status`
（依据 ST 1.18.0：该端点的 MAKERSUITE 分支由服务端去拉 `{reverse_proxy}/v1beta/models?key=…`，
并按 `supportedGenerationMethods.includes('generateContent')` 过滤，只返回能聊天的模型，回包形状 `{ data: [ { id } ] }`），
**走的是同一条反代通道，所以没有 CORS 问题**；直连档则按协议打 `/v1beta/models` 或 `/models`。
模型名输入框同时是下拉框（`<datalist>`）：拉到就自动选中一个 `gemini-2.5-pro/flash`，也能随时手改。
换传输/反代/密码后若模型名仍为空，会自动拉一次。

**关于"生成太慢/太卡"（v1.10.5）**：慢的主要来源是"每段都串行排队"——每一段都要重新付一次首字节延迟。现在改为：
- **流式传输下：基调先行 + 并发生成**。先生成一个 ~900 字的「设计基调」（一组 CSS 变量 `--rx-bg/--rx-accent/--rx-text/--rx-font…` + 风格注释），
  然后**并发（上限 3）生成其余所有样式块**，每个块都引用基调里的变量，保证配色一致；总耗时从「各段之和」变成「最慢的一段 + 一次基调」。
- **流式传输下：实时预览**。CSS 每收一段就立刻组装进骨架渲染给你看，不等全部完成。
- **停顿止损**：流若 20 秒没有新字节（中转卡住但不断开），自动判停、**保留已收到的内容**、立即进入下一段，不再无限等待；
  单次调用还有 4 分钟绝对上限与「收够即停」硬顶（maxChars / maxTokens 按段预算换算），防止模型跑飞。
- **心跳提示**：状态行每 5 秒刷一次"流式接收中… N 字符（Ns，仍在等待…）"，不会再有"看起来像卡死"的静默期。
- 非流式（酒馆主 API）仍走原来的顺序小段 + 自适应缩段（没有并发资本，安全第一）。

配套：**同一份 SSE 解析器同时吃两种形状**（OpenAI 的 `choices[0].delta.content` 与 Google 原生的 `candidates[0].content.parts[].text`），三条路共用收流逻辑。

**另外**：CSS 生成的系统提示已从"带整本世界书（最多 3 万字）"砍到 **484 字符**（只带 500 字气质摘录）——提示词越大，首字节越慢，这本身也是 524 的推手之一。

---

## 七、生成规则要点（注入的世界规则摘要）
- 属性 = 天赋基础 + 层级固定 + 等级额外；开局统一 25 点天赋基础（`basePoints`，每项 0–6，五维和=25）；等级额外 = Lv−1（`attributePoints`）；层级点 = 生命层级(一~七)−1 只结算面板不写表；
- 玩家开局最高等级 **10**（只会处于第一/二/三层级：一_普通 Lv.1-4、二_中坚 Lv.5-8、三_精英 Lv.9-12）；层级点 +0/+1/+2；五维单值不超所在层级极值（一≤8/二≤10/三≤12），单项逻辑上限 20（仅第七层级可达）；等级须由背景人设决定，不乱定级；
- 品级严格七等：普通/优良/稀有/史诗/传说/神话/唯一，对应英文 common/uncommon/rare/epic/legendary/mythic/only；导出 JSON 的 rarity/品质 字段只允许这七个英文之一（导出前自动规范化并对无法识别值告警）；词条上限按品质 普1/优良2/稀有2/史诗3/传说3/神话3，史诗/传说/神话三词条其一须为 微弱要素/微弱权能/微弱法则；FP 即 `reincarnationPoints`（1000-9999）；
- 技能分**攻击技**（消耗[攻击]、即时伤害）与**动作技**（消耗[动作]、禁即时伤害与威力，用于治疗/控制/增益/减益/功能）；威力仅攻击技必填、动作技禁用，核心功能“伤害”仅攻击技可用；
- 装备不增减持有者属性；金额统一用 **Z** 计价（如“资产估价: 数值Z 当地货币名”）；资产/伙伴/背景须字段完整且自洽（≥500字/200字等要求按卡片模板）。

## 八、兼容性与限制
- 需要 SillyTavern 1.12+（依赖 `SillyTavern.getContext().generateRaw`）；不支持时状态条会提示“主API 未就绪”；
- **直连主 API 模式不会执行世界书里的 EJS/MVU 脚本**（已知取舍），悬浮窗只负责“创作内容 + 按新格式汇总 JSON”；
- **本地缓存存于浏览器 localStorage**（键 `openingPresetForge_worldcache_v1` / `openingPresetForge_chardraft_v1`）：换浏览器/清除站点数据会丢失；世界书缓存超过约 4.5MB 时自动精简未勾选条目内容（勾选条目完整保留，重新导入文件即可刷新）；
- 全程单线程串行请求（不并发），步骤间可停止；
- 单文件、零第三方依赖、零相对 import；仅用 `getContext()` 公开面 + jQuery/toastr 全局；每次改动都过 **ES 模块语义校验**（防止重复声明导致整包不加载）。

## 九、版本历史（简）
| 版本 | 内容 |
| --- | --- |
| v1.11.0 | 正则工坊新增两条收尾能力：**①「🔧 自动修复」**（自检后两级修复——本地纯字符串瞬时修 `g`/`.*`/`<script>`/uuid/`promptOnly`/`placement`，剩余需改写的项按条目合并成一次 AI 调用逐条修，改完自动重跑自检；自检区每条另带「✨ AI 修这条」，每张卡另带「🔧 只修这一条」）；**②「✨ 生成后修改」**（每张卡可写自然语言要求改已生成结果，范围可选「只改样式」或「样式+匹配式」，另有「💡 建议」先给 2~3 条可点方向；生成条目只替换 CSS 层、HTML 骨架与 `$n` 引用锁定，手写替换体自动切整段替换模式，防止改坏结构） |
| v1.10.8 | 查明「酒馆里都是流式、插件却非流式」的原因：`openai.js` L2724 的 `stream = settings.stream_openai && type !== 'quiet'`，正常聊天 type=normal 为 true，而 `generateRaw` 走 type=quiet 恒为 false（请求体里就是 `"stream": false`）；新增「📋 用酒馆的反代设置」一键从 `#openai_reverse_proxy` / `#openai_proxy_password` 读取地址与密码并自动切到流式档、自动拉模型列表 |
| v1.10.7 | 生成传输默认改为「酒馆主 API（零配置·推荐）」：不选流式档就**完全不显示**地址/密钥/模型字段，无需任何配置；自定义字段渐进披露 + 状态行提示缺项；主 API 路径用 `responseLength` 按段预算硬顶输出；配置不完整时不再静默改写用户设置，仅本次生成降级并提示 |
| v1.10.6 | 模型列表改双通道合并：ST `/status`（会被 `supportedGenerationMethods` 过滤掉新版 Gemini 模型）+ **直接向反代要原始 `/v1beta/models`**（不过滤，CORS 被拦则跳过）；手填模型名自动记住并进入下拉；状态行报告「原始 N / ST 过滤后 M / 自定义 K」与失败通道原因 |
| v1.10.5 | 提速：流式传输下「设计基调 + 并发 3 路生成样式块 + 实时预览」；流停顿止损（20 秒无新字节即判停并保留已收内容、4 分钟绝对上限、maxChars/maxTokens 按段预算硬顶）；状态行 5 秒心跳；非流式路径保持原顺序小段策略 |
| v1.10.4 | 模型名自动获取：服务端转发档调 `POST /api/backends/chat-completions/status`（ST 服务端经同一反代拉 `/v1beta/models` 并按 `generateContent` 过滤），直连档按协议打 `/v1beta/models` 或 `/models`；模型名输入框改为「可下拉可手填」，拉取后自动选中 `gemini-2.5-pro/flash`；换传输/反代/密码时若模型名为空自动拉取 |
| v1.10.3 | 正则工坊新增「经酒馆服务端转发 + 流式」传输（`POST /api/backends/chat-completions/generate` + `stream:true` + `reverse_proxy`/`proxy_password`/`use_sysprompt`），直击类反向代理场景下的 Cloudflare 524：无 CORS 问题、协议转换与密钥由酒馆服务端处理、真流式回传；「生成传输」改为三档下拉（酒馆主 API / 服务端转发+流式 / 浏览器直连+流式），直连支持 OpenAI 兼容与 Google 原生双协议；SSE 解析器同时兼容 OpenAI 与 Google 两种分片形状 |
| v1.10.2 | 针对 524 的**根因修复**：查明 `generateRaw` 在 ST 源码里固定走 `sendOpenAIRequest('quiet', …)`、而流式分支明确排除 `quiet`（`script.js` L4018 / L5326），即**永远非流式**；据此新增「🚀 直连 + 真流式生成」（插件内 `fetch /chat/completions` + 逐块读 SSE，绕开 100 秒墙）；新增自适应缩段（524 后后续段预算折半，最低 800 字符）；新增「🩺 连通性自检」；524 诊断把 Cloudflare HTML 压成一句人话 + 三条对策；失败保留已生成进度 |
| v1.10.1 | 正则工坊替换体重构为「**结构由代码生成 + 样式按档位分段**」：骨架（含 `$n` 捕获组引用）由插件生成、模型只写 CSS；档位决定段数（1/3/4/6 段）与每段预算（1.3k~3.5k 字符）；花括号净差检测截断并自动续写（≤3 轮）；识别可重试错误退避重试；CSS 生成不再注入整本世界书；新增实时进度状态行 |
| v1.10.0 | 新增「⑥ 与始弦聊天」：完全独立的面板（自有消息数组不写酒馆记录、自有 `{{user}}` 称呼、自有世界书选择、自有存储）；记忆三层（滚动窗口 + 模型压缩的记忆条目 + 本地向量检索）；向量走**自定义 OpenAI 兼容接口**（填 Base URL/密钥 → 读 `/models` → 选模型）；消息/记忆/向量存 IndexedDB；世界书「勾选=总是注入，检索覆盖全馆藏」；她以**同伴观察者 + 全馆藏查询**定位与 user 一起旁观世界 |
| v1.9.1 | 正则工坊的语言格式解析升级为 **AI 解析 + 脚本四道校验**（范例逐字比对防编造、HTML 标签黑名单、枚举去重、生成正则后当场试跑），AI 不可用或结果不可用时自动回退「⚙ 脚本解析（离线）」；结果面板标明所用引擎 |
| v1.9.0 | 新增「⑤ 正则工坊」：核心语言格式解析（兼容 markdown 标题式与缩进式、支持一核心多套格式与多说话人）→ 匹配式由插件确定生成 → 替换体由模型按 12 条硬约束产出 → 实时渲染预览框 → 自检（每条问题带「改正则」「改核心」）→ 导出单条/批量 JSON；预算档位改为目标值制（极简 1.5k/轻量 3k/标准 6k/精致 10k/豪华 20k/**不设限**）；实测世界里 18/22 个核心可生成且范例 100% 命中 |
| v1.8.1 | 命定系统最终封装改为 **YAML 框架 + 条目正文**（框架由插件确定生成：注释名/constant/position/depth/order/disable/selective/probability/key/系统名/系统核心名/系统核心/fp定义/篇幅/正文块标量，模型只写正文，减少一整类失败）；新增《输出格式·硬约束》全程注入（禁 `#` 标题、`**` 加粗、`*` 列表、`---` 分隔线、markdown 表格）+ 5 项装饰 lint（跳过 setvar 槽位）+ 「🧹 去装饰」一键清理 + 「署名」输入框 |
| v1.8.0 | 新增「④ 命定系统工坊」：7 段分段初稿（骨架与命名→契约与核心机制→FP→命定之灵→语言格式→十个变量槽→可选扩展）→ 交火梳理（8 条联动链，两步式）→ 定点精修 → 条目封装；内置 20+ 项 lint（包裹标签与「系统名」一致性、setvar 齐全与顺序、消耗档位形状、撞名、复活约束句、长度预算、人设六字段、功能八族覆盖等）；标准由世界书 22 个命定系统条目普查提炼，可「📐 标准速查」在线展开；新增可选「🧬 进阶：EJS 重型核心」——追加 3 段 EJS 流水线 + EJS 规范注入 + 8 项 EJS 专用 lint，依据艾莉亚核心与读者核心逆向；独立草稿缓存键；占位页顺延为 ⑤DLC剧情 ⑥DLC物品 ⑦更多功能 |
| v1.7.6 | 交火梳理改两步式（先报告后逐段应用，修复截断）；「名称/品质/叙述」三段式字段锁定写入二创全局规则，分段与最终输出不再出现 类型/消耗/标签；YAML 自检新增三字段告警 |
| v1.7.5 | 二创与开局预设提示词完全隔离：二创侧改用独立角色口吻与独立规则块，不再注入开局预设规则（修复二创产出“开局剧情”的问题） |
| v1.7.4 | 「用词与文风规范」同步注入开局预设生成流程（生成初稿/快出/精修/建议/自检/重汇总的系统提示） |
| v1.7.3 | 二创内容规范改版：删「面板」模块（游玩时 AI 自由结算）；武器/装备/道具/技能简化为 名称+中文品质+叙述；去掉对user态度；新增内置「用词与文风规范」（9条）压制八股 |
| v1.7.2 | 二创页「交火梳理报告/最终稿件」改为带框文本框（边框+底色+自动换行+限高滚动，修复文本溢出） |
| v1.7.1 | 二创角色最终输出规范化为 YAML：固定 schema（顶层「角色卡」+ 字段顺序固定）、```yaml 代码块输出、轻量 YAML 自检提示 |
| v1.7.0 | 全屏多页工作台（手机端适配）+ 世界书页迁入 + localStorage 缓存（世界书/勾选/二创草稿跨会话恢复）+ 二创角色工坊（7段初稿→交火梳理→定点修改→最终封装）+ 3 个占位页 |
| v1.6.0 | 适配世界书 v4.3(6)：注释层级标签（[本体]/[DLC]）优先归版、地区二级索引改取标签；规则注入与生成规范同步新版（攻击技/动作技、词条上限、Z 货币、硬顶20、极值仅约束NPC） |
| v1.5.0 | 新增「网游核心·文本总结」独立模块：任务/副本/高难BOSS机制 YAML 摘要 + 可视化 + 按角色/对话持久化（默认关闭） |
| v1.3.3 | 地区·地理按国家/大地区二级分组索引 |
| v1.3.2 | 组织/产业标题例外校准（按用户清单 12+15 条） |
| v1.3.1 | “其他”再梳理：新增 导览开关/组织·势力/经济·产业/节庆；其他 109→0 |
| v1.3.0 | DLC角色并入角色；DLC扩展/DLC事件独立；多本世界书叠加导入 |
| v1.2.8 | 世界书板块“全开/全关” |
| v1.2.7 | 命定系统独立板块；板块展开/收起；窗口与字体调大 |
| v1.2.6 | 世界书条目板块化分类（规则·系统/地区/种族/角色/DLC等） |
| v1.2.5 | 修复根因：移除模块级重复函数声明（此前导致图标/窗口消失） |
| v1.2.4 | 世界书条目=独立左缘侧栏（懒加载，不动主窗口布局） |
| v1.2.2→v1.2.1 | 分步精修 + 重新汇总；回退稳定化 |
| v1.1.0 | Agent 式方向建议（后改分步精修） |
| v1.0.x | 初版悬浮窗/居中修复/属性计算新版规则/改名“始弦的魔法大典” |

## 十、常见问题
- **主 API 未就绪**：升级 SillyTavern ≥1.12 并先正常连接一次主 API；
- **✦ 图标消失**：先在 Extensions 管理器 **Delete 重装**并 Ctrl+F5；若仍异常请把 F12 控制台报错发到 issue（重点看是否 `[openingPresetForge]` 相关或红色 SyntaxError）；
- **生成返回空/未连接**：先在酒馆正常发一条消息确认主 API 可用；
- **世界书自动探测失败**：正常降级，用“导入世界书文件”更稳；
- **JSON 校验提示缺字段**：点“重汇总”或调整该模型输出；
- **更新后行为没变**：先 Delete 重装再 Ctrl+F5。

## 十一、依据（版本相关事实）
- 扩展规范 / manifest / hooks / `getContext()`：官方 `https://docs.sillytavern.app/for-contributors/writing-extensions/`
- `getContext()` 暴露符号（`generateRaw` 等）：SillyTavern `staging` 分支 `public/scripts/st-context.js`
- 世界书模块（`world_info`/`selected_world_info`/`loadWorldInfo`）：SillyTavern `staging` 分支 `public/scripts/world-info.js`

---
仓库：https://github.com/AkarisRotile/opening-preset-forge
如有条目分类想修正、UI/流程想调整，欢迎提 issue 或直接反馈。