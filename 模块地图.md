# 模块地图 · 改哪一页只读哪一块

> **为什么有这份东西**：插件原本是单个 7157 行 / 43 万字符的文件，改任何一页都得整份读，
> 结果就是把 A 页的提示词改到 B 页、把上下文串台。现在源码按**页**切成 14 块放在 `src/`，
> 分发/安装仍然是**单文件**（酒馆只加载 `index.js`）。

## 铁律

1. **改哪一页，只读/只改那一块**（下表）。跨页改动才同时打开两块，并在提交信息里写明。
2. **改完必须构建**：`node tools/build.mjs` → 它把 `src/*.js` 拼回 `index.js`（并同步分发镜像）。
3. **构建后必须过闸门**：`node tools/build.mjs --check`（产物与源码是否同步）+ 跑测试。
4. `index.js` 是**产物**，不要直接改它——下次构建就覆盖了。
5. 分块顺序 = 文件名排序；**启动代码必须留在 `99-boot.js`**（函数声明会提升，但 `var` 初始化按顺序执行）。

## 分块表

| 文件 | 行区间（原始行号） | 对应页面 / 内容 | 什么时候读它 |
| --- | --- | --- | --- |
| `00-header.js` | 1–16 | 文件头、总说明、`'use strict'` | 极少 |
| `10-base.js` | 17–593 | 环境探测、设置存取、`ST` 状态、卡片提示词、上下文构建、`callModel`、通用工具、世界书文件解析 | 改全局工具 / 模型调用时 |
| `30-preset-ui.js` | 594–999 | **① 开局预设**：页面 UI、分步管线、每步精修、重新汇总 | 改 ① 页时 |
| `32-world.js` | 1000–1298 | **② 世界书**：左缘勾选侧栏、分类/搜索、尺寸 CSS | 改 ② 页时 |
| `34-preset-tools.js` | 1299–1845 | **① 开局预设**：合规自检修复、备忘录、品质规范化 | 改 ① 页的合规/备忘录时 |
| `40-shell.js` | 1846–2032 | 分页外壳（`PAGE_DEFS`/建壳/切页）+ 本地缓存 | 改分页/缓存时 |
| `50-char.js` | 2033–2393 | **③ 二创角色工坊** | 改 ③ 页时 |
| `60-destiny.js` | 2394–3251 | **④ 命定系统工坊**：标准、EJS 规范与 lint、脚本封装、AI 检查 | 改 ④ 页时 |
| `70-refine-core.js` | 3252–4112 | **⑦ 命定核心精修**：结构体检、行级 diff、保真校验、锚点定位、补丁应用 | 改 ⑦ 的分析/校验逻辑时 |
| `72-regex-parse.js` | 4113–4516 | **⑤ 正则工坊**：初始框架目录（`RX_FRAMES`）与框架标准、语言格式解析（xml/引语/方括号/裸引号）、匹配式生成、自检、硬约束 | 改 ⑤ 的解析/匹配式/自检/框架时 |
| `74-refine-ui.js` | 4517–5074 | **⑦ 命定核心精修**：补丁解析（分块/JSON）、界面与事件、缓存 | 改 ⑦ 的界面/流程时 |
| `76-regex-main.js` | 5075–6652 | **⑤ 正则工坊**：三档传输、骨架（按框架搭）、CSS 分段生成、自动修复、AI 解析 | 改 ⑤ 的生成/骨架/传输时 |
| `90-shixian.js` | 6653–7132 | **⑥ 与始弦聊天**（独立面板、IndexedDB、向量检索） | 改 ⑥ 页时 |
| `99-boot.js` | 7133–7157 | 启动（`boot` / `tryBoot` / DOMContentLoaded 挂钩） | 极少 |

> ① / ⑤ / ⑦ 各占两个文件，是因为代码在原始文件里本来就交错。**编号即拼接顺序**，不要改名打乱顺序。

## 常用命令

```powershell
# 构建（拼回单文件 + 语法检查 + 顶层重复声明检查 + 同步分发镜像）
node tools/build.mjs
# 只检查"产物是否与源码同步"（提交前/改完先跑这个）
node tools/build.mjs --check

# 闸门
node tools/verify-destiny-page.js     # ④/⑤/⑥/⑦ 接线验收
node tools/test-refine-flow.mjs        # ⑦ 精修（补丁/锚点/保真/分步/截断）
node tools/test-ejs-spec.mjs           # EJS 规范与 lint
node tools/test-dest-assemble.mjs      # ④ 脚本封装（22 个真实核心往返）
node tools/test-regex-workshop.mjs     # ⑤ 正则工坊（真实核心解析）
node tools/test-rx-frames.mjs          # ⑤ 初始框架（4 条现网正则为准：匹配式/骨架/自检/提示词）
node tools/test-rx-autofix.mjs         # ⑤ 自检自动修复 + 生成后修改
node tools/test-rx-transport.mjs       # ⑤ 三档传输与守卫
node tools/test-ai-parse.mjs / test-sse-stall.mjs / test-destiny.mjs / test-shixian.mjs
```

`tools/` 只在开发工作区，不随仓库分发。

## 分发物与源码的关系

- **安装/分发**：`opening-preset-forge/`（单文件 `index.js` + `manifest.json` + `README.md` + 三份规范文档）+ `opening-preset-forge.zip`。**不含本文件与 src/**。
- **源码**：与本文件同级的 `src/`（14 个分块）。
- 构建脚本会把拼好的 `index.js` 同时写入插件根目录与 `opening-preset-forge/`，两边永远一致（`--check` 会验）。
- 修改插件行为时：**先改 `src/`，再构建**，永远不要反向修改 `index.js`。
